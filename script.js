//Напишите функцию, которая принимает два числовых аргумента и возвращает наименьшее из них.

// function Numbers (a,b) {
//     if (a > b ) {
//         return b;
//     } else if (b > a )
//         return a;
// }

// console.log (Numbers (45,44));

// //Напишите функцию, которая принимает два числовых аргумента и выводит в консоль все четные числа от большего к меньшему.
// function EvenNumbers(a, b) {
//     let max, min;
  
//     // Определяем большее и меньшее число с помощью условного оператора
//     if (a > b) {
//       max = a;
//       min = b;
//     } else {
//       max = b;
//       min = a;
//     }
  
//     // Начинаем цикл с большего числа и идем к меньшему
//     for (let i = max; i >= min; i--) {
//       if (i % 2 === 0) {
//         console.log(i);
//       }
//     }
//   }
  
//   // Пример вызова функции
//   EvenNumbers(17, 4); 

  //Напишите функцию power, которая принимает два числовых аргумента (основание степени и саму степень) и возвращает число в указанной степени. Значение степени должно быть указано по умолчанию 2.

  //---не знаю как делать//

 // Напишите функцию, которая принимает числовой аргумент n и считает сумму чисел от 1 до n.
//  function sumN(n) {
//     let sum = 0;
//     for(let i =1; i<= n; i++) {
//         sum += i;
//  }
//  return sum;
// }
// console.log(sumN(100))

//Напишите функцию, которая принимает два числовых аргумента n и m и считает сумму четных чисел и нечетных чисел от n до m. Суммы выведите в консоль (в начале сумму четных чисел, а затем сумму нечетных).

// function sumNM(n,m) {
//   let sumE = 0;
//   let sumO = 0;

//   let start = num.min(n,m);
//   let end = num.max(n, m);
//   //
// for (let i = start; i<= end; i++) {
//     if(i%2 === 0){
//       sumE +=i;
//     }else {
//       sumO +=i;
//     }
    
//   }
//   console.log("Сумма четных чисел: " + sumE);
//   console.log("Сумма нечетных чисел: " + sumO);
// }

// sumNM(1,15);


// function sumEvenAndOdd(n, m) {
//   // Инициализация сумм
//   let sumEven = 0;
//   let sumOdd = 0;

//   // Определение начального и конечного значений
//   let start = Math.min(n, m);
//   let end = Math.max(n, m);

//   // Подсчет сумм четных и нечетных чисел
//   for (let i = start; i <= end; i++) {
//     if (i % 2 === 0) {
//       sumEven += i;
//     } else {
//       sumOdd += i;
//     }
//   }

//   // Вывод сумм в консоль
//   console.log("Сумма четных чисел: " + sumEven);
//   console.log("Сумма нечетных чисел: " + sumOdd);
// }

// // Примеры вызова функции
// sumEvenAndOdd(1, 10);  // Вывод: Сумма четных чисел: 30, Сумма нечетных чисел: 25

//Напишите функцию, которая принимает в качестве аргументов массив строк, а возвращает первый самый длинный элемент массива. Если входной массив пуст, функция возвращает null. Если есть несколько одинаковых по длине элементов - функция возвращает первый из этих элементов. 
Пример: [ 'one', 'two', 'three' ] => 'three'

